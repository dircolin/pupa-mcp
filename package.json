{
  "name": "pupa-mcp",
  "version": "0.1.1",
  "description": "PUPA (PUPASTAGE) MCP server — Korean screenplay → shot plan → AI image/video generation with WOON credit ledger. Use from Claude, ChatGPT, Cursor.",
  "type": "module",
  "bin": { "pupa-mcp": "./bin/pupa-mcp.mjs" },
  "main": "./src/server.mjs",
  "scripts": {
    "start": "node src/server.mjs --http",
    "stdio": "node src/server.mjs --stdio",
    "test": "node test/smoke.mjs"
  },
  "engines": { "node": ">=18" },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "zod": "^3.25.0"
  }
}
